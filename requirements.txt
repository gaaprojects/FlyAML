# Local dev mirror of the pyproject dependencies (Python 3.12 venv).
# Kaggle installs the package itself with:
#   pip install "git+https://github.com/gaaprojects/FlyAML.git@<tag>"
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
python-igraph>=0.10
scikit-learn>=1.3
matplotlib>=3.7
pytest>=7.0
